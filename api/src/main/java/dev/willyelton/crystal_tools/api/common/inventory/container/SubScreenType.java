package dev.willyelton.crystal_tools.api.common.inventory.container;

// TODO: Make this not an enum so that other mods can add sub screens
public enum SubScreenType {
    NONE, COMPRESS, FILTER, QUARRY_SETTINGS, SIDE_SETTINGS
}
